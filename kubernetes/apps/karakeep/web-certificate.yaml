apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: karakeep-internal-certificate
spec:
  secretName: karakeep-internal-certificate
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - karakeep.internal.dmhosted.com
