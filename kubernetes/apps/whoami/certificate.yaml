---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: whoami-public-cert
  namespace: whoami
spec:
  secretName: whoami-public-cert
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - whoami.dmhosted.com
