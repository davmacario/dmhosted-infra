apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homepage-cert-internal-root
  namespace: homepage
spec:
  secretName: homepage-cert-internal-root
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - internal.dmhosted.com
