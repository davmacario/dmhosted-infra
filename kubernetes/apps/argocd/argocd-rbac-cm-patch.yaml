# Patch for the `argocd-rbac-cm` configmap (manages RBAC for users)
apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
data:
  policy.csv: |
    g, homepage, role:readonly
