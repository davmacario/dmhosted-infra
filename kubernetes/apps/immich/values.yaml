# yaml-language-server: $schema=https://raw.githubusercontent.com/immich-app/immich-charts/refs/heads/main/charts/immich/values.schema.json
controllers:
  main:
    containers:
      main:
        image:
          # Deployed immich version - see https://github.com/immich-app/immich/releases
          tag: v3.0.2
        env:
          REDIS_HOSTNAME: '{{ printf "%s-valkey" .Release.Name }}'
          IMMICH_MACHINE_LEARNING_URL: '{{ printf "http://%s-machine-learning:3003" .Release.Name }}'
          # Env vars to connect to database (cnpg cluster)
          DB_HOSTNAME: immich-db-rw
          DB_DATABASE_NAME: immich
          DB_USERNAME: immich
          DB_PASSWORD:
            valueFrom:
              secretKeyRef:
                name: immich-app-db-secret
                key: password
        resources:
          requests:
            cpu: 250m
            memory: 128Mi
          limits:
            cpu: 2000m
            memory: 400Mi
    replicas: 1
    strategy: RollingUpdate

immich:
  metrics:
    # Creates ServiceMonitors
    enabled: true
  persistence:
    # Main data store for all photos shared between different components.
    library:
      existingClaim: immich-data
  # Existing configuration resource name to skip creation of a new one
  # config file ref: https://immich.app/docs/install/config-file/
  existingConfiguration: immich-app-config

# Dependencies
valkey:
  enabled: true
  controllers:
    main:
      containers:
        main:
          image:
            repository: docker.io/valkey/valkey
            tag: 9.1-alpine@sha256:a35428eba9043cc0b79dbe54100f0c92784f2de00ad09b01182bfb1c5c83d1bd
            pullPolicy: IfNotPresent
      strategy: Recreate
  persistence:
    data:
      enabled: true
      # Keep job queues persistent
      type: persistentVolumeClaim
      size: 1Gi
      accessMode: ReadWriteOnce
      storageClass: local-path

# Immich components
server:
  enabled: true
  controllers:
    main:
      containers:
        main:
          image:
            repository: ghcr.io/immich-app/immich-server
            pullPolicy: IfNotPresent
  ingress:
    main:
      enabled: false
      annotations:
        # proxy-body-size is set to 0 to remove the body limit on file uploads
        nginx.ingress.kubernetes.io/proxy-body-size: "0"
      hosts:
        - host: immich.local
          paths:
            - path: "/"
              service:
                identifier: main
      tls: []

machine-learning:
  enabled: true
  controllers:
    main:
      containers:
        main:
          image:
            repository: ghcr.io/immich-app/immich-machine-learning
            pullPolicy: IfNotPresent
          env:
            TRANSFORMERS_CACHE: /cache
            HF_XET_CACHE: /cache/huggingface-xet
            MPLCONFIGDIR: /cache/matplotlib-config
          resources:
            requests:
              cpu: 500m
              memory: 256Mi
            limits:
              cpu: 3000m
              memory: 1024Mi
      strategy: Recreate
  persistence:
    cache:
      enabled: true
      # Avoid downloading the ML models on every start
      type: persistentVolumeClaim
      size: 10Gi
      accessMode: ReadWriteOnce
      storageClass: longhorn-singlecopy # Using longhorn because of data locality - new pods using this will be scheduled on node with volume
