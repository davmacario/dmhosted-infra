# Networking configuration on k8s

The manifests found here are used to set up networking in the homelab.

Specifically, I am using the `networking` namespace to set up Traefik routes to endpoints outside of K8s.
